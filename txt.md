# AUTH AND SECUIRTY

- conditional rendering of page
- encrypting value using `md5`
- encrypting values using `bcrypt | bcryptjs`
- implementing login, register and logout using:
  - `passport`
  - `passport-local`
  - `express-session`
- generating, decoding and passing token using `JWT`
